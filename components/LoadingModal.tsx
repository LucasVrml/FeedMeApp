import { LoadingSpinner } from "./LoadingSpinner";
import Modal from "./Modal";

const LoadingModal = () => {
  return (
    <Modal>
      <LoadingSpinner />
    </Modal>
  );
};

export default LoadingModal;
